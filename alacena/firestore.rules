rules_version = '2';
// Desarrollo alpha — acceso abierto para uso familiar local
// IMPORTANTE: restringir antes de producción real
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
